System:

  Security:

    dmUser:
      name:             User
      credentials:      security_user
      components:
        signin:
        form:           { name: "Register" }
        forgotPassword:

    dmPermission:
      name:             Permission
      credentials:      security_permission

    dmGroup:
      name:             Group
      credentials:      security_group