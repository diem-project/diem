rendering:    ~

remember_me:
  class:      sfGuardRememberMeFilter

security:
  class:      dmSecurityFilter

dm_init:
  class:      dmFrontInitFilter

cache:        ~

common:
  enabled:    false

execution:    ~